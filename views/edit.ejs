<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
          .container {
      border: 1px solid;
      border-color : dimgray;
      padding : 5px; /*untuk ngasihh lebar background*/
      margin: 5px;
      justify-content: center;
      border-radius : 15px;
      text-align: center;
    }
        </style>
<title>Bootcamp Project</title>
</head>
<body>
    <div class="container">
    <h2>Edit Data Siswa : <%= data.id %></h2>
    <form action="/admin/edit/<%= data.id %>" method="POST">
        <label for="name">Nama:</label>
        <input type="text" id="name" name="name" value = "<%= data.name %>" required />
        <label for="tgl_lahir">Tanggal Lahir:</label>
        <input type="text" id="tgl_lahir" name="tgl_lahir" value = "<%= data.tgl_lahir %>"/>
        <label for="kelas">kelas:</label>
        <select id="kelas" name="kelas" required>    
            <option value="IPA" "<%=data=kelas="IPA" ? "selected" : "" %>" >IPA</option>
            <option value="IPS" "<%=data=kelas="IPS" ? "selected" : "" %>">IPS</option>
            <option value="MTK" "<%=data=kelas="MTK" ? "selected" : "" %>">Matematika</option>
        </select>
        <label for="alamat">Alamat:</label>
        <input type="text" id="alamat" name="alamat" value= "<%= data.alamat %>" required/>
        <label for="no_hp">Nomor Handphone:</label>
        <input type="text" id="no_hp" name="no_hp" value= "<%= data.no_hp %>" required/>
        <label for="nama_ortu">Nama Orangtua:</label>
        <input type="text" id="nama_ortu" name="nama_ortu" value= "<%= data.nama_ortu %>" required/>
        <label for="no_hp_ortu">No. hp. Orangtua:</label>
        <input type="text" id="no_hp_ortu" name="no_hp_ortu" value= "<%= data.no_hp_ortu %>" required/>
        <button type="submit">Update</button>
    </form>
    </div>
</body>
<footer>
</footer>
</html>